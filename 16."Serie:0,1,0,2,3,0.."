#include <iostream>
using namespace std;
int main() {
    int n;
    cout << "Ingrese n: ";
    cin >> n;

    for (int i = 0; i <= n;i++) {
      cout << (i % 4 == 0 ? 0 : (i / 4) * 3 + (i % 4)) << ",";
    }
    return 0;
}
